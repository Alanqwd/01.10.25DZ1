<Window x:Class="_1._10._25Dz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_1._10._25Dz"
        mc:Ignorable="d"
        Title=" Список дел" 
        Height="450" Width="400"
        Icon="/Image/icon.png">
    
    <Window.Resources>
        <Style x:Key="HeaderStyle">
            <Setter Property="Control.FontFamily" Value="Cascadia Mono"></Setter>
            <Setter Property="Control.FontSize" Value="26"></Setter>
            <Setter Property="Control.FontWeight" Value="Bold"></Setter>
            <Setter Property="Control.Padding" Value="5"></Setter>
            <Setter Property="Control.Margin" Value="5"></Setter>
        </Style>
    </Window.Resources>



<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0">
            <TextBlock  Style="{StaticResource HeaderStyle}" Foreground="#5ebec4">Список дел</TextBlock>
        </StackPanel>

        <Button HorizontalAlignment="Left" Grid.Column="1" Grid.Row="0"  Name="buttonAdd" Click="AddCase" Background="#5EBEC4" Height="30" Width="60" Margin="135,5,0,5">
            <Image Source="Image/add.png" Height="25"></Image>
        </Button>

    
        <DataGrid GridLinesVisibility="Horizontal" HeadersVisibility="None"   Name="DataGridToDo" AutoGenerateColumns="False" Background="#FDF5DF" Grid.ColumnSpan="2" Grid.Row="1" Grid.Column="0"  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn/>
                <DataGridTextColumn IsReadOnly="True" Width="*" Binding="{Binding Path=CaseName}" />
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Path=TimeCompleating, StringFormat=dd.MM.yyyy}" />
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Grid.Column="0" Grid.Row="2" Name="buttonDel" Click="DelCase"  Background="#5EBEC4" Height="15" Width="15" Margin="5">
                                <Image Source="Image/delete.png" Height="10"></Image>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <TextBox TextWrapping="Wrap" IsReadOnly="True" MaxLines="8" Width="370" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Text="{Binding Path = Description}"></TextBox>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>
        </Grid>
        
</Window>
