<Window x:Class="_1._10._25Dz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_1._10._25Dz"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>

        </Grid.ColumnDefinitions>

        <TextBlock  Grid.Row="0" Grid.Column="0" Text="Список дел" FontSize="26" HorizontalAlignment="Left" />
        <ListBox Grid.Row="1" Grid.Column="0" x:Name="List" Background="#FDF5DF" ItemsSource="{Binding Path=CasesList}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5" Background="#FDF5DF">
                        <TextBlock Width="1000" Text="{Binding Path=CaseName}" FontSize="16"/>
                        <TextBlock Width="1000" Text="{Binding Path=DateExecution}"/>
                        <TextBlock Width="1000" Text="{Binding Path=DescriptionCase   }"/>

                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>


        <Button Grid.Column="0" Grid.Row="2" Content="Удалить дела"  Background="#5EBEC4" Height="30" Width="100" HorizontalAlignment="Left" Margin="50 1 1 1"/>
        <Button Grid.Column="0" Grid.Row="2" Click="Button_CLick" Content="Добавить дело"  Background="#5EBEC4" HorizontalAlignment="Right"  Height="30" Width="100" Margin="1 1 50 1"/>
    </Grid>
</Window>